# server/.env

# Server Configuration
PORT=8080
CLIENT_URL=http://localhost:3000 # Or your React app's URL

# Database Configuration (Match db.config.js and your actual DB)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=test12345
DB_NAME=keka_clone_db
DB_PORT=5432

# JWT Configuration
JWT_SECRET=your-very-strong-and-secret-jwt-key # Choose a strong random secret
JWT_EXPIRES_IN=86400 # Optional: Token expiry in seconds (e.g., 86400 = 24 hours)